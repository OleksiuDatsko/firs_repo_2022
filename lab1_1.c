//my firs coment

#include <stdio.h>
#include <math.h>

void main(){
    double z = 9.761;
    double res = pow(z, 6) + 13*pow(z, 4) + 7*pow(z, 3) + 14*pow(z, 2) - 2*z + 21;
    printf("%f", res);
} 
